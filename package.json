{
  "name": "composio-research-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI-assisted connector research agent and case study for the Composio Product Ops take-home assignment.",
  "scripts": {
    "parse": "tsx src/parseAssignment.ts",
    "research": "tsx src/research.ts",
    "verify": "tsx src/verify.ts",
    "verify:requirements": "tsx src/verifyRequirements.ts",
    "verify:urls": "tsx src/verifyEvidenceUrls.ts",
    "build": "npm run build:site",
    "build:site": "tsx src/buildSite.ts",
    "proof:mcp": "tsx src/proofMcp.ts",
    "review:gemini": "tsx src/geminiLiteReview.ts",
    "check": "tsc --noEmit",
    "all": "npm run parse && npm run research && npm run verify && npm run build:site && npm run verify:requirements && npm run build:site",
    "audit": "npm run parse && npm run research && npm run verify && npm run build:site && npm run verify:requirements && npm run verify:urls && npm run build:site && npm run verify:requirements && npm run check"
  },
  "dependencies": {
    "@ai-sdk/mcp": "^2.0.9",
    "@composio/core": "^0.13.1",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.0.10"
  }
}
